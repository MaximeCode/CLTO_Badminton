server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Ancien site PHP → nouvelles pages
    location ^~ /news/ {
        return 301 https://cltobadminton.fr/actualites;
    }

    location ^~ /club {
        return 301 https://cltobadminton.fr/;
    }

    # Autres préfixes anciens fréquents (à adapter selon GSC)
    # location ^~ /news_controller/ { return 301 https://cltobadminton.fr/actualites; }

    location / {
        try_files $uri $uri/ /index.html;
    }
}